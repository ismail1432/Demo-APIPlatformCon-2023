{% extends 'base.html.twig' %}

{% block title %}Hello UtilisateurController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Utilisateurs !</h1>

    <ul>
        <hr>
        {% for utilisateur in utilisateurs %}
            <li>Nom : {{ utilisateur.nom  }} </li>
            <li>Prenom : {{ utilisateur.prenom}} </li>
            <li>Adresse : {{ utilisateur.adresse.rue}} </li>
            <li>Ville : {{ utilisateur.adresse.ville }} </li>
            <li>Code Postal : {{ utilisateur.adresse.codePostal}} </li>

            {% if  utilisateur.commandes|length > 0 %}
            Commandes
            {% endif %}
            {% for commande in utilisateur.commandes %}
                <li>{{ commande.nom }} {{ commande.prix }} €</li>
            {% endfor %}
        <hr>

        {% endfor %}
        <ul>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/Users/smaine/project/demo_api_platform_con/src/Controller/UtilisateurController.php'|file_link(0) }}">src/Controller/UtilisateurController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/Users/smaine/project/demo_api_platform_con/templates/utilisateur/index.html.twig'|file_link(0) }}">templates/utilisateur/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
